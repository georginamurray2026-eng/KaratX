# Carried-forward obligations

The tracked ledger of what is owed, where it lands, and what has been discharged. Lessons live in [LESSONS.md](./LESSONS.md); current state lives in [STATUS.md](./STATUS.md).

**Split out of `docs/STATUS.md` on 2026-09-02 — obligation 25.** STATUS.md had
reached 3,247 lines of nested headings and pipe-delimited tables and had produced
four anchor-collision incidents, the last of them miscounting obligations by 11
because a regex matched three other tables. This file and its sibling are read
independently of the handoff and are edited most often by script, so separating
them makes scripted edits scope-safe by construction rather than by discipline.

**Content below is moved VERBATIM.** No wording was changed in the split; that
was deliberate, so this commit is reviewable as a move rather than as a rewrite.

## Carried-forward obligations


**21 open, 19 discharged**, as of 2026-09-04. Counted by listing the numbered
rows of THIS table only: 3, 4, 5, 7, 9, 11, 12, 16, 17, 19, 22, 24, 27, 31, 32,
34, 39, 40, 41, 42, 43.

**Obligation 43 was raised 2026-09-04**, out of the T1.4 backfill estimate. It
is the reason the count moved from 20.

**This count has been wrong before** — it read 21 here while a summary elsewhere
said 22 — which is part of why obligation 25 split this file out and why no
summary is kept outside it. Where the open ones land:

| Lands in | Obligations |
| --- | --- |
| **T1.3** | 31 (flag only) |
| **T1.4** | 31 (evidence), 41 |
| **T1.4, T1.7** | 5 |
| **T1.5** | 39, 40 |
| **before obligation 40** | 42 |
| **T1.7 — firm** | 22 |
| **T6.1** | 3, 4, 17, 19, 24, 27, 32 |
| **before Phase 2** | 11, 12, 43 |
| **before Phase 6** | 16 |
| **OVERDUE — was due at/before T1.2** | none — 10 and 25 both discharged 2026-09-02 |
| **next session** | none |
| **standing** | 34 |
| **unscheduled** | 7, 9 |

**The table's entries sum to 22 for 21 obligations, deliberately.** Obligation 31
is listed TWICE — T1.3 for the flag, T1.4 for the evidence — because it is split
across two tasks. Any other disagreement between this count and this table is a
defect; this one is the split being visible.

### Blocking dependencies between obligations — the convention

**A dependency is recorded IN THE BLOCKED ROW AND IN THE BLOCKING ROW, both
directions, and is NOT represented in the landing table above.** Reading either
row alone tells you the whole relationship; the table answers "when does this
land", which is a different question and stays that way.

**Two exist today:**

- **42 blocks 40** — the Pine export row cap must be measured before the longer
  capture that would settle the daily-break disagreement can even be planned.
- **43 blocks 12** — venue divergence must be measured before parity's
  tolerance can be stated as a number.

**IF A THIRD ARRIVES, THE QUESTION TO ASK IS WHETHER THIS TABLE NEEDS A
DEPENDENCY COLUMN — not whether to add another prose note.** A third note is
the point at which the convention has become "whatever the last person did".
Hand-maintained derived structure drifting out of step with the table it
describes is this file's own recorded failure mode: it is exactly why the
bullet breakdown below was deleted.

**The bullet-list breakdown that stood here until 2026-09-02 was STALE and is
replaced.** It still named "T0.10" categories after ADR-011 re-dated those to
T6.1, and it counted the lettered rows (10b–10e) that the sentence above
explicitly excludes — so its totals never matched its own stated method. A
derived list maintained by hand inside a 3,200-line document drifts from the
table it summarises; that is obligation 25's argument, restated by example.

Sorted ascending. Discharged obligations are struck through and kept, so a
later session can see that a question was asked and answered rather than
wondering whether it was ever considered.

| # | Obligation | Lands in |
|---|---|---|
| ~~1~~ | ~~**`packages/core` import-boundary regression test.**~~ **DISCHARGED in T0.6.** `packages/test-support/src/core-boundary.test.ts` lints snippets through the ESLint API against a virtual `packages/core` path and asserts each rule fires. Proven by deliberately weakening `eslint.config.js` two ways: changing the core block glob failed 11 of 15 tests, removing the empty-catch selector failed exactly 1. Both reverted, confirmed with `git diff`. **Its failure messages explain why the boundary exists and say not to delete the test** — the real risk is removal by someone who does not know what it protects | done |
| ~~2~~ | ~~**Prevent modification of existing migration files on `main`.**~~ **DISCHARGED 2026-08-28.** `scripts/check-migrations-immutable.mjs` runs in CI and was proven by a deliberate break (run 33103674481): `FAILED: migrations already on main were changed. MODIFIED: packages/db/migrations/0000_init_system_events_and_config.sql`, compared against a REAL `origin/main` at `3cd3f930085f`. The rule is deliberately not "nothing under migrations/ may change" — `.sql` and snapshots are byte-immutable, `_journal.json` is append-only, because a blunt rule would reject every legitimate new migration | done |
| 3 | **Verify Railway's pre-deploy/release migration mechanism.** Migrations must be a release step, never the service start command (OPS-2). How Railway expresses that is `[VERIFY]` — unconfirmed, `ARCHITECTURE.md` U-7  | **T6.1 — was "T0.10". The mechanism is DOCUMENTED (ADR-003 amendment, from vendor docs) but has never been verified against a real deploy, and there is none until T6.1** |
| 4 | **Railway backup and restore (OPS-7).** Requires a *tested* restore, not just a configured backup. Entirely outstanding  | **T6.1 — was "T0.10". **The LOCAL half is DISCHARGED** — backup and restore built and drilled in T0.10 L3, `pnpm db:drill` exit 0. The HOSTED half needs a deployment** |
| 5 | **"Avoid infinite retry loops" (§23).** T0.5 defines the `retry` policy but **nothing consumes it**. It lands in T1.4 backfill and T1.7 reconnection, both of which specify bounded exponential backoff with jitter | **T1.4, T1.7** |
| ~~6~~ | ~~**No ADR records the TypeScript 6.0.3 pin.**~~ **DISCHARGED — verified 2026-08-31.** ADR-007 "TypeScript pinned to 6.x" is Accepted, dated 2026-08-26, names `^6.0.3` twice, and records both the typescript-eslint reason and the originating commit `d41b2cb`. **It had been satisfied for five days while still listed as open** — the obligations table had never been reconciled against the artefacts it tracks | **CLOSED 2026-08-31** |
| 7 | **ESLint flat-config trap.** Flat config *replaces* a rule's options rather than merging. Any future repo-wide `no-restricted-syntax` rule must be repeated inside the `packages/core` block or it silently switches off there. Verify with `eslint --print-config` | ongoing |
| ~~8~~ | ~~**`ARCHITECTURE.md` §D is wrong and needs correcting.**~~ **DISCHARGED in T0.8.** §D no longer says "holding a websocket" and carries a dated correction note; §E/U-1's matrix row now records that the question is resolved for OANDA and that its original answer ("websocket is better for sweep detection") was exactly backwards for this provider — kept as a criterion because it still applies to evaluating a replacement. The F.2 amendment, which had flagged both as uncorrected, now says both were fixed | done |
| 9 | **Migration CLI duplicates redaction logic.** `packages/db/src/bin/migrate.ts` hand-rolls error redaction predating T0.5's logger. **Its `.env` loading was deduplicated in T0.8** — that copy and three others now share `loadEnvFileIfPresent` in `@karatx/config` — but the redaction itself is untouched | low priority |
| ~~10~~ | ~~**The CSV fixture loader does not handle quoted fields containing commas.**~~ **DISCHARGED 2026-09-02.** `readCsvFixture` now REFUSES any line containing a double quote rather than mis-parsing it, and the error says why and says not to add partial quote handling. **A refusal, deliberately NOT a quote parser** — the obligation's own argument is that a half-correct parser is worse than none, and that argument does not stop applying because we are the ones writing it. **The gap was DEMONSTRATED, not asserted:** the new fixture `sample/quoted-comma.csv` splits into exactly 5 values against a 5-column header, so the existing column-count check does NOT fire — it parsed "successfully" with `high` receiving `637.29"` and `close` silently receiving `low`'s number. **Proven by mutation:** with both guard calls removed, the refusal test fails and the other 12 still pass, which is the evidence that the old path was silent rather than merely untested. A positive control asserts the ordinary unquoted fixture still parses, so the guard is not a blanket refusal | **DISCHARGED 2026-09-02 — was "T1.2", OVERDUE** |
| ~~10a~~ | ~~**REQUIRED, not optional: a CI job that runs unit tests with NO PostgreSQL service attached.**~~ **DISCHARGED 2026-08-27.** The `unit` job declares no `services:` block AND asserts nothing is listening on 5432 before running — omission is invisible, an assertion fails loudly if someone adds one. Green on CI #1 and every run since | done |
| 10b | **Integration isolation is per *run*, not per *file*.** Each run gets its own ephemeral database, so two runs — CI and local, or two CI jobs — cannot collide. **Within** a run, files still share that one database and rely on `fileParallelism: false`. Revisit per-worker schemas if the integration suite becomes slow in Phase 1 | if suite slows |
| 10c | **A pre-T0.6 leftover database `karatx_test` exists on the local server.** It does not match the current anchored naming pattern, so the sweep will correctly never touch it — "unrecognised means untouched". It is harmless clutter from the T0.4 scheme and can be dropped manually whenever convenient | cosmetic |
| 10d | **Do not replace the crash-path test with a "more realistic" kill test.** `db.integration.test.ts` reproduces the post-crash state deterministically via `KEEP_TEST_DB=1`, which skips teardown and leaves exactly the database a crashed run leaves behind. Two attempts at a timing-based kill were tried first and neither was valid — one finished before the kill landed, the other killed a worker while Vitest's main process survived and cleaned up anyway. A timing-based test would be **flaky forever**, and a flaky test around destructive operations is worse than none. Reproducing the state that matters beats simulating the event that causes it | do not "fix" |
| 10e | **Vitest cleans up after a worker crash — the orphan window is narrower than assumed.** Measured during T0.6: killing a test *worker* leaves Vitest's main process alive, which reports `Worker exited unexpectedly` and still runs `globalSetup` teardown, dropping the database. So a crashed test does not usually orphan anything. **The 24-hour floor is still justified**, because it covers the cases teardown genuinely cannot run: machine reboot, a cancelled CI job, SIGKILL of the whole process tree, and Docker stopping underneath a running suite. Those are the real orphan sources | rationale |
| 11 | **PHASE 2 PREREQUISITE — RE-MEASURED 2026-08-31, and the number DOUBLED.** Windows wall clock for `pnpm test` is now **38.1 s** (239 tests), against **18.2 s** at `66be0e4` (132 tests) and **4.3 s** on Linux in CI. Per-test cost barely moved (~138 ms → ~159 ms); the absolute cost roughly doubled because the suite did. **The cause is unchanged and confirmed: `pnpm -r` spawns a Vitest process per package, and process spawn is expensive on Windows** — Linux per-package durations total well under 4 s, so almost none of it is spawn there. **Fix: a single Vitest workspace run sharing one process.** Removal was proposed on the grounds that the 18.2 s figure was stale; re-measuring showed the concern had GROWN, so removal would have retired a live problem on a number too old to justify either decision | **before Phase 2 — number is now current as of 2026-08-31** |
| 12 | **C3 indicator parity still needs TradingView's own computed values, and there is no route to them yet.** TradingView's *Export chart data* is a paid feature the user does not have, so the golden CSV planned for T0.6/T1.10 could not be produced. Without TradingView's own EMA and Stoch RSI numbers there is nothing to assert engine output *against*, and audit finding C3 — parity within a documented tolerance — cannot be closed by inspection alone. **Routes being explored, in order:** (1) Pine Script `log.info()` output, which would let the chart emit its own indicator values; (2) one month of a paid TradingView plan purely to run the export; (3) manual transcription of ~20 bars, enough for a spot check but not a fixture. **Blocks nothing in Phase 0 or Phase 1.** Required before **Phase 2** indicator work begins. **UPDATED 2026-09-02 — ROUTE 1 IS PROVEN AND THE FIXTURES ARE CAPTURED. THIS OBLIGATION STAYS OPEN, AND THE REASON IS THE WHOLE POINT OF IT.** Pine `log.info` works: historical bars DO log, `t` is a plain 13-digit integer, `i` is consecutive, and 299 bars per timeframe are committed at `test/fixtures/tradingview/` with 8-10 decimal places against the legend's 3. The recorded blocker — "TradingView's export is a paid feature we do not have, so there is nothing to assert against" — **is resolved.** Route 2 (buy a month) and route 3 (transcribe 20 bars) are no longer needed. **WHAT REMAINS IS THE THING THE OBLIGATION IS ACTUALLY FOR: PARITY. Nothing has been asserted yet.** This row exists because audit finding C3 "cannot be closed by inspection alone" — and having something to assert against is NECESSARY AND INSUFFICIENT, which is this project's own phrase for exactly this position. Closing it now would record C3 as satisfied while zero comparisons have been run, and the first thing Phase 2 would do is discover that. **Discharge condition, narrowed:** the engine reproduces these fixtures within a documented tolerance, at all three timeframes, with the tolerance stated as a number rather than as "close enough". Obligation 41 records the history the engine must be fed first. **UPDATED 2026-09-04 — THAT DISCHARGE CONDITION IS NOW BLOCKED BY OBLIGATION 43.** The tolerance has to be "a number rather than close enough", and that number cannot be derived until OANDA-vs-Twelve-Data divergence is measured: these fixtures are one venue's chart and the ingestion feed is another's, and the gap between them has never been measured. A tolerance chosen without it either hides a real implementation bug inside venue noise or fails an engine that is correct. **This does not stop a 15m parity run happening first** — see obligation 43 for what such a run still tells us | **before Phase 2 — NARROWED 2026-09-02 from "no route to golden values" to "parity not yet asserted"; BLOCKED BY 43 since 2026-09-04** |
| ~~13~~ | ~~**T0.9 must set `NEXT_TELEMETRY_DISABLED=1` in the CI environment.**~~ **DISCHARGED 2026-08-27.** Set at workflow level so no job can forget it, and also pinned in `playwright.config.ts`'s webServer env | done |
| ~~14~~ | ~~**`pnpm typecheck` has a blind spot — audit it.**~~ **DISCHARGED 2026-09-01 in `c6e4538`.** Audited by MEASUREMENT, not by reading include globs: `tsc --listFilesOnly` across all nine projects listed **80 of the repository's 90** TypeScript files. The ten uncovered were all build/test config — nine vitest configs and `packages/db/drizzle.config.ts`. Every package includes `src/` and sometimes `test/`; config sits outside that and no project owned it. **A type-stripping loader is not a typechecker** — vitest and drizzle-kit load these through esbuild/tsx, which deletes types without checking them, so "it runs" was never evidence. Closed with `tsconfig.tooling.json` + `typecheck:tooling`, and **proven both directions with one planted error**: misspelling `fileParallelism` left `pnpm typecheck` at exit 0 BEFORE and produced TS2769 with a "Did you mean" hint AFTER. That option is not cosmetic — misspelled, the db integration suite went from **28 passed to 4 FAILED**, four files racing one shared database, reported as missing columns with nothing pointing at the config. **`vitest.shared.ts` was covered only by ACCIDENT** — solely via `apps/web`'s `**/*.ts` glob following its config's import upward; the other seven packages import it and reach none of it. Now listed explicitly. Coverage re-measured after: **90 of 90** |
| ~~15~~ | ~~**T0.9 must build `apps/web` BEFORE running integration tests.**~~ **DISCHARGED 2026-08-27.** The integration job applies migrations, then builds `apps/web`, then runs the suite. Both are real dependencies: `apps/web`'s readiness tests read DATABASE_URL directly rather than creating an ephemeral database, and its instrumentation test spawns `next start`, which needs `.next` | done |
| 16 | **`apps/web` computing strategy is unenforced.** F.1 says the web app reads what the worker wrote and never computes. That holds today by inspection only — no rule prevents `apps/web` importing an indicator from `packages/core` and calculating in the dashboard, producing two implementations that drift. The fix has a proven shape: an ESLint boundary plus a regression test, exactly as T0.2/T0.6 did for `packages/core`. Cheap now, and the reason to do it before Phase 6 builds the real dashboard | **before Phase 6** |
| 17 | **T0.10 must re-measure worker boot-failure behaviour against however the worker actually runs on Railway.** T0.8 measured six failure modes under `tsx` **at commit `f9a75a5`** and all exited non-zero, so the worker needs no explicit `process.exit(1)`. **That result is valid for `tsx` only.** If production runs compiled output, a different entry point, or a process supervisor that wraps execution, the measurement must be repeated — believing a tsx result about production would be the minified-name mistake in a different costume. **Also check what Railway does with each exit code**: the lifecycle exits 0 on a clean shutdown and 1 when a hook failed or timed out, and that distinction is only useful if the platform acts on it | **T6.1 — was "T0.10 — firm"; re-dated 2026-08-31 by ADR-011, since there is no deployment to measure against until then** |
| ~~18~~ | ~~**OPS-3 end to end is unproven, and CI is the only place it can run.**~~ **DISCHARGED 2026-08-27 by CI #1, on the step output rather than on a green job** — that distinction is the whole point of the obligation. The *Assert the SIGTERM test actually ran* step printed: `Total assertions in report: 10` / `PASSED: "shuts down cleanly and exits 0" ran and passed (/home/runner/work/KaratX/KaratX/apps/worker/src/boot.integration.test.ts)`. **Ten assertions — the same count as the Windows report where that test showed as `skipped`. The contrast is the evidence.** Run: https://github.com/georginamurray2026-eng/KaratX/actions | done |
| 19 | **`apps/web` resolves the repository root from a BUNDLED module.** Under Turbopack `import.meta.url` points inside `.next/`, so the upward walk for `pnpm-workspace.yaml` may fail. **UPGRADED 2026-08-28 from "watch this" to a defined test** — asking what the symptom would be found a live defect first (see the deferred-risk lesson), now fixed in `fc74b50`. **What to look for in the Railway logs, and what each means:** (a) *no notice* — the walk succeeded, bundling does not break it; (b) *`No repository root found (no pnpm-workspace.yaml above this module)`* — the walk FAILED, which is harmless because Railway injects env vars anyway, but obligation 19 is confirmed real and the message says so; (c) *`FATAL:` and a non-zero exit* — something else broke, and it exits rather than lying. **The silent-pass case no longer exists**: there is no path where a failed walk is indistinguishable from a correctly-absent `.env` | **T6.1 — was "T0.10 — observe on first deploy"; re-dated 2026-08-31 by ADR-011. There is no first deploy until T6.1** |
| ~~20~~ | ~~**Crash logging has never run in a real worker process.**~~ **DISCHARGED 2026-08-28.** `apps/worker/test/crash-harness.ts` installs the REAL `installCrashLogging` and crashes itself; `crash-logging.integration.test.ts` spawns it for both `uncaught` and `unhandled` and asserts it started, did not hang, exited non-zero, and emitted a fatal JSON line carrying `category` and `policy`. **A harness, deliberately NOT a `KARATX_CRASH_TEST` env var** — a production affordance existing only for a test is the wrong artefact and a genuine remote-crash primitive. **Proven by TWO mutations, not one** — see the refinement to the positive-control rule. Leaves obligation 23 | done |
| ~~21~~ | ~~**Pool closure is unverified against a real database.**~~ **DISCHARGED 2026-08-28.** `apps/worker/src/lifecycle.integration.test.ts` opens a real `Pool`, forces three concurrent queries so backends genuinely exist, registers it with a real `Lifecycle` exactly as `boot.ts` does, shuts down, and counts backends in `pg_stat_activity` from a SEPARATE client. **Proven by two mutations, one per assertion:** removing `pool.end()` failed with *"3 backend(s) still attached"* after the full 5s poll; pointing the observer query at a nonexistent `datname` failed the POSITIVE CONTROL, which is the assertion that stops the test passing while verifying nothing. Polls rather than sleeps, and asserts on the last real reading | done |
| 22 | **`isShuttingDown` has no production consumer.** OPS-3's "stops accepting work" is a SEAM, not a behaviour — `grep` finds it referenced only by `lifecycle.ts` and its own tests. Correct today, since there is no work until the feed exists. **T1.7 must wire the feed consumer to check it**, or the clause is never actually satisfied | **T1.7 — firm** |
| ~~23~~ | ~~**Nothing proves `index.ts` WIRES the crash handlers in.**~~ **DISCHARGED 2026-09-01 in `dd03445`**, option 2 as preferred. `packages/test-support/src/wiring-assertions.test.ts` asserts `apps/worker/src/index.ts` calls `installCrashLogging`, and **its failure message says in those words that it checks TEXT, not behaviour** — the recorded condition for accepting the cheap check. **The gap was MEASURED rather than inherited:** with the call commented out, all **33 worker unit tests and all 14 worker integration tests still pass**, including the one that spawns a real process and crashes it. Proven by mutation against the real file, then reverted |
| 24 | **`apps/worker` has NO build step, so nothing verifies it becomes a deployable artefact.** It has no `build` script and runs `tsx src/index.ts`. CI therefore cannot check that it compiles, and T0.9's "build" criterion is unmeetable for it rather than merely narrowed. **T0.10 must decide whether production runs `tsx` or a compiled artefact** — and that decision drives obligation 17 (re-measure boot-failure behaviour against however it actually runs) and the pino-flush scope limit (buffered crash output was proven to survive under `tsx` only). Three findings, one underlying question | **T6.1 — was "T0.10 — firm"; re-dated 2026-08-31. ADR-009 answered the tsx-vs-artefact question, so what remains is verifying a deployable artefact, which needs a deployment** |
| ~~25~~ | ~~**Split STATUS.md.**~~ **DISCHARGED 2026-09-02.** Its own anchor lesson set the trigger "if this happens again, the file is telling us it needs splitting" — and it had happened four times, most recently miscounting obligations by 11 because a regex matched three other tables. STATUS.md had reached **3,248 lines**. Split into `docs/LESSONS.md` (1,309 lines) and `docs/OBLIGATIONS.md` (84 lines, this file), leaving STATUS.md at **1,870** as the handoff document. **Moved VERBATIM — no wording changed in the split**, so the commit is reviewable as a move rather than a rewrite. **Verified three ways rather than by eye:** both moved bodies diffed byte-identical against `HEAD:docs/STATUS.md` at the extracted line ranges; the 1,854 retained lines diffed byte-identical outside the two stubs; and the line accounting balances (3,248 − 1,309 − 84 + 15 stub lines = 1,870). The extraction script asserted each range began on its expected heading and ended before a separator, and would have refused to write otherwise | **DISCHARGED 2026-09-02 — was "before T1.2", OVERDUE** |
| ~~35~~ | ~~**`Dependabot Updates` has been RED since 2026-08-29 — the proactive half of SEC-10 is not running.**~~ **DISCHARGED 2026-09-01, and THE RECORDED HYPOTHESIS WAS WRONG.** Reading the run log first — as the obligation instructed — refuted it in one step: the scheduled `npm_and_yarn in /. - Update #1` run **SUCCEEDED** on 2026-08-31 (id 33350101588), and an unparseable lockfile would have failed that run too. **Discovery was never lost.** All three red runs (33105196543, 33184569966, 33254585583) are named `npm_and_yarn in /. for esbuild` — the SECURITY update for GHSA-67mh-4wv8-2f99, a different job — and they failed because there was no update to make: `drizzle-kit@0.31.10` is the LATEST release, every `0.31.x` depends on the DEPRECATED `@esbuild-kit/esm-loader`, and that chain pins `esbuild@0.18.20`. Fixed in `7754628` by the scoped override `@esbuild-kit/core-utils>esbuild: ^0.25.0` — scoped because a bare `esbuild` override would have DOWNGRADED vite 8 from 0.28.2. `pnpm audit` went 1 moderate → 0, with the before-run as its positive control. **NOT PROVEN: that the job turns green.** The cause is removed, but that is an assertion about a future run — see obligation 36. **`7754628` was LOCAL-ONLY until 2026-09-02**, so for the whole period this row described the fix as made, it was invisible to GitHub and to Dependabot. **DIAGNOSIS CONFIRMED COMPLETE 2026-09-02** — obligation 36 observed alert #1 CLOSED AS FIXED after a rescan that postdates the push, so the scoped override was the whole fix and not merely part of it. The "NOT PROVEN" caveat above is now discharged; it stays written because it was the correct thing to say at the time | **CLOSED 2026-09-01; diagnosis confirmed 2026-09-02** |
| ~~37~~ | ~~**Enable GitHub secret-scanning PUSH PROTECTION — it is free, and it is off.**~~ T0.9 recorded gitleaks as DETECT-not-PREVENT and accepted that gap because push protection "needs paid Secret Protection on private repositories". **The repository is PUBLIC** (verified unauthenticated 2026-09-01), and secret scanning with push protection is free on public repositories. So the accepted limitation is not a limitation, it is an unset toggle. gitleaks scans history AFTER a push; push protection blocks the push. **They are complementary — do not remove gitleaks.** Requires a settings change by the repository owner: Settings → Code security → Secret protection. **DISCHARGED 2026-09-02, ON EVIDENCE — ENABLED *AND* PROVEN ENFORCING.** Both toggles were turned on by the owner, and the enforcement test was then RUN rather than assumed, because "enabled" and "enforcing" are not the same claim. Two fake baits on a throwaway branch, never on `main`: a **GitHub PAT** in classic format with a computed CRC32 checksum, and a **Slack bot token** shape. Both randomly generated, never issued, never sent to any API, nothing from `.env`. **The push was REJECTED with `GH013 ... push declined due to repository rule violations`, and GitHub named BOTH baits** — "GitHub Personal Access Token" at `push-protection-probe.txt:14` and "Slack API Token" at line 18 — so both were evaluated, not just the first. `git ls-remote` confirms no ref ever reached the remote; `main` never moved from `dab1998`. No bypass URL was visited. **THE BAIT WAS PROVEN DETECTABLE BEFORE THE PUSH, and that is what makes an answer possible at all.** `gitleaks` exit 1, two findings, `github-pat` and `slack-bot-token`, values redacted. Without that pre-flight an accepted push would have been ambiguous between "not enforcing" and "bait never detectable" — which is the T0.9 `AKIAIOSFODNN7EXAMPLE` mistake in a new costume: a clean result from a scanner that could not have matched. **Two baits deliberately**, one checksummed and one pattern-only, so that an accepted push could still distinguish a wrong checksum from absent detection. It did not come to that. **gitleaks STAYS — the two are complementary, not redundant.** gitleaks scans history AFTER a push and across every ref; push protection blocks the push before anything lands. Removing either leaves a real gap | **DISCHARGED 2026-09-02 — was "next session — needs the owner"** |
| ~~36~~ | ~~**Confirm `Dependabot Updates` is actually GREEN.**~~ **DISCHARGED 2026-09-02, ON EVIDENCE.** Observed at GitHub → Security → Dependabot alerts after the push: **0 Open, 1 Closed** — *"esbuild enables any website to send any requests to the development server and read the response"* (Moderate), alert #1, **CLOSED AS FIXED**, detected in `esbuild` (npm) via `pnpm-lock.yaml`, with *"Dependency files checked 50 minutes ago"*. **Three things make this EVIDENCE rather than inference, and all three are load-bearing:** (1) **CLOSED AS FIXED, not dismissed** — a human dismissal produces an identical "0 Open" count and means nothing, so the closure REASON is the observation, not the count; (2) **the rescan timestamp POSTDATES the push**, so GitHub read the lockfile CONTAINING the override rather than the one before it — a pre-push rescan would have looked the same and proved the opposite; (3) it is a **POSITIVE observation about the alert**, not an absence of red runs. **That third point is the whole reason this obligation existed.** The esbuild security job will now never run again, because there is nothing left to detect — so silence was always going to be the outcome whether the fix worked or not, and "no red runs" could never have distinguished the two. The alert state can. **What the LOCAL check did and did not prove:** `pnpm audit` going 1 moderate → 0 showed what OUR resolver saw when it read the lockfile. GitHub's dependency graph is a DIFFERENT READER of the same file, with its own advisory database and its own resolution. Their agreement is the thing that was unproven until now — the local run was never evidence about GitHub's view, only about ours | **DISCHARGED 2026-09-02 — was "next session — firm"** |
| 31 | **`db:restore` atomicity is OBSERVED, not GUARANTEED.** Five deliberate failures on 2026-08-30 all exited non-zero and left the database intact — but that is because `pg_restore` reads the archive table of contents before applying anything, so a 5 KB dump fails before the first `DROP`. **On a multi-gigabyte Phase 1 dump, corruption late in the data stream could fail after earlier statements have committed**, leaving the half-populated database the drill was designed to rule out. `--exit-on-error` makes it stop; it does not make it undo. **Fix: add `--single-transaction`, then re-run the deliberate failures against a dump large enough that the failure lands mid-restore** — the evidence must come from a dump where the old behaviour would actually have differed. **SPLIT 2026-09-02 (ADR-013 session), and the split is the point: DO NOT MARK THIS COMPLETE BECAUSE THE FLAG EXISTS.** T1.3 creates the candles table; T1.4 fills it. At T1.3 the database still holds only reference rows, so a dump fails before the first statement applies and the new behaviour is indistinguishable from the old — adding the flag would demonstrate nothing. `--single-transaction` also implies `--exit-on-error`, and is compatible with the existing `--clean --if-exists` because no `--jobs` is used. **FLAG ADDED 2026-09-02 in the T1.3 sequence — AND THIS OBLIGATION REMAINS OPEN.** The flag is in `scripts/db-restore.mjs` with the reasoning at the call site, including what is still unproven. **Re-running the 2026-08-30 deliberate failures now would demonstrate NOTHING:** they passed under the old flags too, because the dump was too small to fail mid-restore, so old and new behaviour are indistinguishable at this size. That is the whole reason the evidence half waits for T1.4 | **OPEN. Flag added T1.3 2026-09-02; EVIDENCE at T1.4, against a post-backfill dump** |
| 32 | **ADR-003’s forward-compatibility amendment does not deliver a usable rollback, and this is now MEASURED.** `packages/db/src/status.ts` computes `inSync` as `pending.length === 0 && unknown.length === 0`, where `unknown` means the database is AHEAD of the code. Proven 2026-08-31 with the most forward-compatible change that exists — an additive nullable column: old code against the newer schema returns **503 `not_ready`**, `expected 503 to be 200`. Since the healthcheck is a DEPLOY GATE, a rolled-back deployment would never go live, so rollback-without-restore is unavailable for any change containing a migration. **Two coherent resolutions, and the choice is real:** (a) the readiness check is right, forward compatibility cannot deliver rollback, and every migration-bearing rollback is a restore — say so in ADR-003 rather than implying otherwise; (b) `unknown` should NOT block readiness, because being ahead is tolerable when migrations are additive, in which case `pending` alone gates and the amendment becomes real. **Do not decide this by editing the code first** | **T6.1 — firm; revisit ADR-003 in the same breath** |
| 39 | **`pnpm db:migrate` prints `Migrations applied.` and does not say WHAT it applied, or how many.** A run that applied NOTHING produces byte-identical output to one that applied three migrations — the exact shape this project keeps catching, and the reason the 0002 verification on 2026-09-02 was only real because the `drizzle.__drizzle_migrations` ledger was queried SEPARATELY. The exit code and the message were both satisfied by a run that could have done nothing. **Fix: name each migration applied and print a count, and say "no migrations to apply" when there are none — the two cases must not look alike.** `packages/db/src/bin/migrate.ts:33`. Raised as its own number rather than left as a note, because that is precisely what happened to the restore message: it sat as an unnumbered "Owed" line from 2026-08-31 to 2026-09-02 and nothing scheduled it | **T1.5 — before the next migration is applied. Deliberately NOT folded into T1.3** |
| 42 | **The Pine export row cap is UNMEASURED, and obligation 40 cannot be settled until it is.** All three 2026-09-02 captures returned **299 data rows + 1 header** while the script requested `logLastNBars = 300`. Identical counts across three different timeframes is not chance — but **every capture requested 300**, so "the cap is exactly 300" has never been distinguished from any other explanation. It is currently recorded in INDICATOR-SPEC.md as an INFERENCE, and until this runs it must stay one. **THIS BLOCKS OBLIGATION 40.** Settling the OANDA-vs-Massive break disagreement needs ten or more daily breaks. At the ~93 fifteen-minute bars per trading day that migration 0001 records, ten breaks is **~930 bars and realistically ~1,300 for margin** — three to four times what one export returned. A longer capture is a precondition for that measurement, so the cap has to be known first. **THE TEST, and it is one run:** set `logLastNBars = 500` in `tools/tradingview/karatx-golden-export.pine`, re-export 15m, count the data rows. **500 rows → there is no 300 cap** and the earlier short count has another cause. **Still 299 → the cap is 300 total rows including the header**, and it is a property of the export rather than of the request. **FALLBACK IF THE CAP IS REAL, recorded now so it is not rediscovered under time pressure:** a long capture must be STITCHED from several overlapping exports, scrolling the chart back between runs and joining on `i`. The overlap is what makes the join verifiable — consecutive `i` across the seam proves nothing was dropped, which is the same check that validated the first three files. Do not stitch on timestamp; `i` is the field that makes a gap detectable. **Raised as its own number rather than left as a note in INDICATOR-SPEC**, because that is exactly what happened to the restore message: it sat as an unnumbered "Owed" line from 2026-08-31 to 2026-09-02 and nothing scheduled it, because nothing could | **BEFORE obligation 40's capture. One run, and it unblocks the calendar measurement** |
| 40 | **The OANDA 15m daily break looks like 60 MINUTES; migration 0001 seeds 45.** The seed records the break as `daily_break 17:00-17:45 America/New_York`, **measured against Massive on 2026-08-27** and explicitly "45 MINUTES, not the 60 of the CME futures convention". The 2026-09-02 OANDA 15m capture shows **1.25 h between bar opens across both daily breaks** — 16:45 opens, then 18:00 opens, so four 15-minute slots are missing, which is 60 minutes. **TWO OCCURRENCES IS A FLAG, NOT A CONCLUSION** — the capture spans Fri 28 Aug to Wed 2 Sep, so only two daily breaks fall inside it. **WHY IT MATTERS MORE THAN A 15-MINUTE DISCREPANCY SOUNDS:** `market_hours` is the authority T1.5 asks "how many bars should exist on this date?". If the calendar says 45 and the venue does 60, T1.5 reports three missing bars **every single trading day** — a false data-quality event per day, forever. BUILD-PLAN already names this risk in T1.5: "false positives here will train you to ignore the alerts." A control that cries wolf daily is worse than no control. **WHAT WOULD SETTLE IT:** a longer 15m capture covering ten or more daily breaks, or the same boundary compared across OANDA and Massive on identical dates. **THE LONGER CAPTURE IS BLOCKED ON OBLIGATION 42** — ten breaks is ~930 bars at 93 per trading day, and one export returned 299, so the row cap has to be measured before a capture of that length can be planned. Either distinguishes a venue difference from a measurement error. **AND THE SCHEMA MAY NOT BE ABLE TO EXPRESS THE ANSWER.** `market_hours` has `instrument_id` but no provider or venue column, so it cannot currently hold "45 minutes on Massive, 60 on OANDA". If the difference is real the calendar must become PER-VENUE, and that is a migration on a reference table plus a change to every query that reads it. **Do not seed a fix before the measurement** — the current 45 is itself a measurement, and replacing one under-evidenced number with another is not progress | **T1.5 — before the calendar is used to judge anything** |
| 41 | **The parity test needs 1000 bars of history BEFORE the first golden bar, and nothing currently says so.** INDICATOR-SPEC requires 5x the period before an EMA is trustworthy; for EMA200 that is 1000 bars. The golden fixtures are TradingView's **expected OUTPUT** and contain no input history — `i` starts at 5903 / 9575 / 14541, so TradingView computed those values over thousands of prior bars we do not have. **Feeding the engine only the 299 fixture bars would produce EMA200 values that are wrong and look plausible**, and the parity test would fail for a reason that has nothing to do with the implementation. **Concretely, backfill must reach at least:** 15m from **~2026-08-13**, 1H from **~mid-June 2026**, 1D from **~2021** (1000 daily bars is roughly four years). **Twelve Data's 15M depth begins 2020-01-24 per ADR-008, so all three are covered** — this is a sizing requirement, not a blocker. It is written down here so it is not rediscovered as a mysterious parity failure. **UPDATED 2026-09-04 — SIZED AGAINST THE FIXTURES, AND ONE ASSUMPTION IN THIS ROW INVERTED.** Re-derived from the fixtures' actual first bars — 15m `2026-08-28T09:45Z`, 1H `2026-08-14T15:00Z`, 1D `2025-07-06T21:00Z` — the dates above reproduce, so the sizing stands. **What did not survive is the unstated assumption that 1D would be DERIVED from the 15M spine.** This row specifies BAR COUNTS, and 1000 + 299 = 1299 daily bars is ~5 years; five years of 15M is ~135,000 bars, which is **27–36 requests against 1 request to fetch 1299 daily bars natively.** Deriving is roughly **10x dearer here, not 3x cheaper** — the "a third of the requests" intuition holds for a fixed CALENDAR WINDOW, not for a fixed BAR COUNT, and obligation 41 specifies the latter. **DECIDED 2026-09-04: the 1H and 1D parity inputs are FETCHED, and the 15M backfill takes the FULL 6.6 years rather than the ~20-day minimum.** The full run costs ~9 requests more than the minimum once 1D is out of the 15M spine, and carrying two windows to reason about later is the worse trade at that price. **FETCHING 1D DOES NOT REVERSE ADR-008 OR T1.6, AND MUST NOT BE READ THAT WAY.** The 15M spine and upward aggregation stand unchanged; ADR-008 already provides for fetching the provider's own aggregate as a **regression assertion rather than as a source**, and this is that same mechanism used for a second purpose. A later session finding a fetched 1D series should read it as parity input, not as the ADR being quietly abandoned. **AND 1D PARITY IS BLOCKED ON T1.5's CALENDAR WHATEVER THE BACKFILL SIZE** — the 1D fixture window 2025-07-06 → 2026-08-31 sits **entirely inside Twelve Data's weekend-synthesis era**, so a Twelve Data daily series over it carries Saturday and Sunday "days" the fixture does not (299 bars across 421 calendar days is 0.71 — weekday-only). ADR-008 records that consequence for T1.6's regression guard; it applies to a **fetched** 1D exactly as much as an aggregated one, so no amount of backfill unblocks it. Mirrored into INDICATOR-SPEC.md where a parity run will actually be read | **T1.4 — sized 2026-09-04. 15m and 1H parity inputs land here; 1D parity is additionally blocked on T1.5** |
| 43 | **THE GOLDEN FIXTURES AND THE INGESTION FEED ARE DIFFERENT VENUES, AND THE TWO HAVE NEVER BEEN COMPARED.** The fixtures at `test/fixtures/tradingview/` are `OANDA:XAUUSD` captured through TradingView — INDICATOR-SPEC names it as the source of truth. The ingestion feed is **Twelve Data** (ADR-008). A parity test therefore compares indicator values our engine computes on ONE venue's bars against values TradingView computed on ANOTHER venue's bars, and **OANDA-vs-Twelve-Data divergence is UNMEASURED.** The only comparable figure this project holds is Twelve Data against Massive on weekday 15M bars matched by exact UTC timestamp: **9–11% of bar range, 0.8–2.8 bp, roughly $0.20–$1.20 on gold**, measured 2026-08-27 across four days either side of the 2025 boundary. Nothing says OANDA sits closer to Twelve Data than Massive does. **WHY THIS IS NOT A SMALL EFFECT, AND THIS IS THE PART THAT IS EASY TO MISS:** obligation 41 requires 1000 warm-up bars before the first golden bar, and those bars come from the FEED while the 299 fixture bars are TradingView's own. For EMA200, α = 2/201, so the weight still resting on that backfilled prefix is (1 − α)^n — **100% at the first golden bar, and ~5% by the 299th.** Early-bar parity is therefore **testing the FEED rather than the arithmetic**, and no quantity of backfill fixes it: more history moves the mixing point earlier, it does not remove it. This is why obligation 41 alone is not enough, even though it looks like it covers the same ground. **THE CONSEQUENCE — IT BLOCKS OBLIGATION 12, NOT MERELY T1.4.** Obligation 12 discharges on a tolerance "stated as a number rather than as close enough", and that number cannot be derived without knowing how far the two venues sit apart. A tolerance picked without it does one of two things, both bad: it is set wide and hides a real implementation bug inside venue noise, or it is set tight and fails an engine that is correct. **WHAT WOULD SETTLE IT:** the same normalised comparison already run for Twelve Data vs Massive — weekday 15M bars matched by exact UTC timestamp, divergence expressed as a **percentage of bar range** rather than in dollars, because the 2026-08-27 measurement showed raw divergence tracks volatility while normalised divergence does not. The fixture rows carry `o/h/l/c`, so the OANDA side of the comparison needs no new capture for the 299 bars already held. **WHAT IT DOES NOT BLOCK, recorded so this does not stall work that can proceed:** a 15m parity run can happen as soon as the engine exists and will still be informative — **if it fails by far more than 11% of bar range, the cause is not venue divergence**, and that is worth knowing before any of this is measured. **RAISED 2026-09-04, out of the T1.4 backfill estimate**, where it surfaced as a side effect of sizing obligation 41 rather than from anyone looking for it | **before Phase 2 — BLOCKS obligation 12** |
| ~~38~~ | ~~**A failed `db:restore` leaves `public` EMPTY and must be run TWICE, and the error message does not say so.** Observed live 2026-08-31: restoring a pre-migration backup over the migrated schema made layer-3 content verification fire correctly and quarantine `public` to `failed_restore_<timestamp>`, leaving an empty `public`; the second invocation restored properly. **This is not merely a message fix.** DEPLOYMENT.md’s documented rollback path trips the two-run behaviour **every time**, because a pre-migration backup by definition has fewer tables than the current schema — and obligation 34 records that the procedure has never been run by anyone who was not present when it was written. **So the first person to follow it cold hits a failure mode the document does not mention, while mid-incident, and would reasonably conclude everything had been lost.** Carried as an unnumbered "Owed" line from 2026-08-31 until 2026-09-02, which is why nothing scheduled it. **DISCHARGED 2026-09-02, before the T1.3 migration** — deliberately in that order, because this is the restore path that guards the first large table and `CLAUDE.md` requires a backup before every migration. The message now states that the data is NOT lost, prints the exact command to re-run, explains why the documented rollback trips this every time, and says that a second failure means something else. DEPLOYMENT.md gained a titled section, and the rollback step warns INLINE — the previous version made the reader find the explanation, which mid-incident they will not | **DISCHARGED 2026-09-02 — was "T1.3 — firm"** |
| ~~33~~ | ~~**ADR-003 is enforced by CONVENTION, not by a check.**~~ **DISCHARGED 2026-09-01 in `dd03445`**, same pass as obligation 23. `wiring-assertions.test.ts` asserts no file under `apps/` imports `runMigrations`, matching the IMPORT rather than the call so a comment mentioning the name is not a false positive. Proven by mutation — planting the import in `apps/worker/src/boot.ts` fails the test and NAMES the file — then reverted. **Being an ABSENCE check it carries its own positive controls:** the same detector must find single-line, multi-line and mixed-specifier imports in synthetic source, must not fire on a comment or a string, and the file walk asserts a non-empty list anchored to a file known to exist. An empty file list answers "nothing found" to every question |
| 34 | **DEPLOYMENT.md’s rollback procedure has never been used by anyone who was not present when it was written.** It was drafted and drilled by the same session, so every ambiguity in it was resolved by knowledge the reader will not have. **There is no honest way to close this except by someone running it cold** — and the first genuine test will be the first real incident. **Listed deliberately as a standing limitation rather than a task**: it is EXPECTED, not a gap to be closed now, and it should stay listed rather than being quietly resolved. When it is first used in anger, record what was unclear | **standing — review after first real use** |
| ~~26~~ | ~~**Confirm the audit step reports without blocking, IN CI.**~~ **DISCHARGED 2026-08-28 by CI #14 at `91b0a55`, on the printed advisory rather than the job colour** — a green security job would have looked identical if the report line had produced nothing. The step printed the full advisory (package, `<=0.24.2`, `>=0.25.0`, the full dependency path, the GHSA link) and then `--- applying the blocking threshold ---`, and the job stayed green. **The `|| true` survived the runner: reporting changed, threshold did not** | done |
| 27 | **NOTHING WATCHES THE HEALTH ENDPOINTS AFTER A DEPLOY GOES LIVE.** Railway queries the healthcheck only while a deployment is going live — *"Railway does not monitor the healthcheck endpoint after the deployment has gone live"*. **The 3am scenario, written out because "we have health endpoints" reads as covered and is not:** the database becomes unreachable; `/api/ready` starts returning 503, correctly; **nothing restarts the service; nothing alerts anybody**; Railway's UI still shows the deployment as live and healthy; the dashboard serves 503s until a human happens to look. The endpoints are correct — nothing is watching them. Needs an external uptime check or a scheduled probe that can page someone  | **T6.1 — was "OPS-8 — firm". Nothing is deployed under ADR-011, so there is no live deployment to watch until T6.1** |
