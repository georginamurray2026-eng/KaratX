{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": "src",
    // Opts in to Node ambient types: this package uses AsyncLocalStorage and
    // writes to stdout. packages/core deliberately does not.
    "types": ["node"]
  },
  "include": ["src/**/*.ts"]
}
