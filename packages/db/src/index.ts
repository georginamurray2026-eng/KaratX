/**
 * `@karatx/db` - Drizzle schema, migrations and query helpers.
 *
 * Populated in T0.4 (system_events, config) and extended through Phase 1.
 * Migrations are a deliberate release step, never implicit at boot (OPS-2).
 */

export const DB_PACKAGE_NAME = '@karatx/db' as const
